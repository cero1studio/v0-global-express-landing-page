/**
 * Configuración fija para export estático (`output: 'export'`).
 * Edita aquí los IDs; no depende de variables de entorno en el build.
 */

/** URL canónica del sitio (sin barra final) */
export const SITE_URL = 'https://globalexpressrecruiting.com'

/** Google Analytics 4 */
export const GA_MEASUREMENT_ID = 'G-42QRG2806R'

/** Google Tag Manager — dejar vacío si no usas GTM */
export const GTM_ID = ''

/**
 * Google Ads — rellena ambos para conversión + conversiones avanzadas en /gracias.
 * Ej.: GOOGLE_ADS_ID = 'AW-123456789'
 *      GOOGLE_ADS_CONVERSION_SEND_TO = 'AW-123456789/AbCdEfGhIjKlMnOp'
 */
export const GOOGLE_ADS_ID = ''
/** Valor completo send_to del snippet de conversión (incluye etiqueta) */
export const GOOGLE_ADS_CONVERSION_SEND_TO = ''
